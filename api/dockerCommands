To build docker image-
docker build -t <image_name> .

To list docker images-
docker images

To build docker container-
docker run --name <container_name> -p <docker_port>:<host_port> -d <image_name>

To start a container
docker start <docker_name>

To stop container-
docker stop <container_name>

To list runnig container-
docker ps

To list all container-
docker ps -a

To delete the image -
docker image rm <image_name> -f(to force)

To delete the container
docker container rm <container_name>

To remove all images, container, volumes
docker system prune -a

To compose docker up-
docker-compose up

To compose docker down-
docker compose down
